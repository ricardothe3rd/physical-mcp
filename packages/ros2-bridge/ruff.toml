# Ruff configuration for PhysicalMCP ROS2 bridge

target-version = "py310"

[lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]  # line length handled separately

[format]
line-length = 120

[lint.isort]
known-first-party = ["physical_mcp_bridge"]
